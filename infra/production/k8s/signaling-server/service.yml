apiVersion: v1
kind: Service
metadata:
  name: signaling-server
  namespace: default
  labels:
    app: signaling-server
spec:
  type: ClusterIP
  selector:
    app: signaling-server
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  sessionAffinity: None


