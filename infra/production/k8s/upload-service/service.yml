apiVersion: v1
kind: Service
metadata:
  name: upload-service
  namespace: default
  labels:
    app: upload-service
spec:
  type: ClusterIP
  selector:
    app: upload-service
  ports:
  - port: 80
    targetPort: 8082
    protocol: TCP
    name: http
  sessionAffinity: None
